# Phụ thuộc phát triển & kiểm thử (pin chính xác).
-r requirements.txt

pytest==8.3.4            # chạy test
httpx==0.28.1            # TestClient cho FastAPI (đã có ở lõi, giữ để rõ ràng)
ruff==0.9.2             # lint + format
mypy==1.14.1            # kiểm kiểu tĩnh
types-PyYAML==6.0.12.20240917  # stub kiểu cho PyYAML
pyinstaller==6.11.1     # đóng gói native theo OS
